# Activar la reescritura de URLs
RewriteEngine On
RewriteBase /clientfinder-ai/

# Redirigir todo a index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
